<section class="section has-background-white box">
  <h1 class="title">Rooms</h1>
  <% unless user_signed_in? %>
    <%= link_to "Sign Up", new_user_registration_path, class: "button mb-5" %>
    <%= link_to "Sign In", new_user_session_path, class: "button mb-5" %>
  <% end %>
  <%= render @rooms %>
</section>
